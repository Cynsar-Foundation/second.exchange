/**
 * The service has to be re-init where the page loads
 * We need to know about certain components like if the user Signer , fetch the profile,
 *
 */

import { NDKSigner } from "@nostr-dev-kit/ndk";

export const reInit = async(signer: NDKSigner, ) => {
    //
}